# LLM-Graph Plugins

This repository groups **official integrations** for the LLM-Graph standard.

Currently included:

- WordPress plugin (injects an LLM-Graph web profile snippet in `<head>`)
- Shopify snippet (Liquid snippet to include LLM-Graph web profile)

Each integration lives in its own subfolder.
